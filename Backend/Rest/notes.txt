REST - Representational State Transfer
An architectural style that defines a set of constraints(rules) to be used for creating web services.

CRUD opreations- 
                GET - retrieves resources
                POST - submits new data to server
                PUT - updates existing data
                PATCH - update existing data partially
                DELETE - removes data

res.redirect(url) - used to redirect web page

CONNNECTING NODE TO SQL -

connection.end() -> to close connection 

usong sql from CLI -> /usr/local/mysql/bin/mysql -u root -p
                    create schema.sql
                    source schema.sql //in CLI